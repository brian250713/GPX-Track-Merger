## 1. 時間統計計算（純函式）

- [x] 1.1 新增 `src/core/dayTime.ts`，輸入 `Day`、輸出出發時間、抵達時間與總時長（毫秒）。驗證：`tests/dayTime.test.ts` 涵蓋 `specs/time-and-pace/spec.md` 的「單一段落」與「含斷開的一天」兩個情境，斷言含斷開的一天總時長仍是首尾相減
- [x] 1.2 行進時間只加總同一段落內相鄰點的時間差，斷開處不計入。驗證：測試涵蓋「斷開處不計入」（5 小時而非 7 小時）、「沒有斷開時等於總時長」與「短休息計入行進時間」三個情境
- [x] 1.3 平均速度以 `day.distanceKm` 除以行進時間（小時）計算。驗證：測試以 110.2 公里、6 小時 20 分斷言結果為 17.4；並斷言分子確實取自 `day.distanceKm` 而非重新累加
- [x] 1.4 行進時間為 0 或點數不足時回傳明確的「無法計算」狀態，而非 `NaN`／`Infinity`。驗證：測試涵蓋「只有一個點」與「行進時間為零」兩個情境，斷言回傳值不含 `NaN` 與 `Infinity`

## 2. 格式化（純函式）

- [x] 2.1 時長格式化：超過一小時顯示 `6 小時 20 分`，不足一小時只顯示 `48 分`。驗證：測試涵蓋兩個情境，並加上 0 分鐘與剛好 60 分鐘的邊界
- [x] 2.2 時刻格式化：以傳入的時區產生 24 小時制的 `HH:mm`，沿用 `grouping.ts` 既有的 `Intl.DateTimeFormat` 快取做法。驗證：以 `2026-03-12T00:12:00Z` 搭配 `Asia/Taipei` 斷言輸出 `08:12`
- [x] 2.3 速度格式化到小數點後 1 位。驗證：斷言 17.44 與 17.45 的輸出符合預期

## 3. 分天摘要整合

- [x] 3.1 在 `src/ui/summary.ts` 的展開區塊中，於高度剖面之外掛上時間統計，每個數值都附有標籤。驗證：測試展開一天後斷言區塊內同時存在剖面 SVG 與五個標籤（出發、抵達、總時長、行進時間、均速）
- [x] 3.2 時間以 `state.timeZone` 呈現。驗證：測試以不同時區的 stub state 斷言顯示的時刻不同
- [x] 3.3 沒有高度資料的一天仍顯示時間統計。驗證：測試涵蓋「沒有高度資料的一天」情境，斷言說明文字與時間統計同時存在
- [x] 3.4 無法計算均速時顯示說明文字而非數值。驗證：測試涵蓋「行進時間為零」情境，斷言畫面不含 `NaN`、`Infinity`
- [x] 3.5 收合列的內容不變。驗證：既有的 `tests/summary.test.ts` 顯示內容測試仍通過，並斷言收合狀態下的列文字不含時刻

## 4. 樣式

- [x] 4.1 在 `src/styles/components.css` 加上時間統計的緊湊排版，沿用既有 design token，不硬寫色值。驗證：`tests/tokens.test.ts` 仍通過
- [x] 4.2 窄螢幕（400px）展開後不出現水平捲軸，統計不與剖面圖擠在一起。驗證：手動以 400px 視窗檢查

## 5. 整體驗收

- [x] 5.1 執行 `npm test`，全部通過
- [x] 5.2 執行 `npm run build`，TypeScript 無錯誤
- [x] 5.3 以 `tests/gpx/` 的真實 GPX 手動驗收：展開各天確認出發／抵達時間符合當地時間、行進時間短於總時長、均速數值合理
- [x] 5.4 執行 `openspec validate day-time-and-pace --strict`，通過
