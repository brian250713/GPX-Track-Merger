## 1. 剖面計算（純函式）

- [x] 1.1 新增 `src/core/elevation.ts`，輸入 `Day`、輸出每個段落的里程–高度點序列與整天的高度極值；里程沿用 `haversineMeters` 逐段累加，斷開處不累計。驗證：`tests/elevation.test.ts` 中單一段落與兩段落的里程值符合 `specs/elevation-profile/spec.md` 的「計算每天的里程–高度剖面」情境
- [x] 1.2 缺少 `ele` 的點不納入剖面點，但仍計入里程累加。驗證：新增測試涵蓋「部分點缺少高度」與「缺少高度的點不扭曲里程」兩個情境，最後一點的里程等於 `day.distanceKm`
- [x] 1.3 可用剖面點少於 2 個時回傳明確的「無資料」結果，而非空陣列或 `NaN`。驗證：測試涵蓋完全沒有高度、只有一個點有高度兩種輸入
- [x] 1.4 高度極值計算與全天高度相同的情況（範圍為 0）。驗證：測試斷言最高/最低皆為該值，且結果不含 `NaN`

## 2. 剖面圖繪製（inline SVG）

- [x] 2.1 新增剖面圖繪製函式，把 1.1 的結果轉成 inline SVG：每個段落一條獨立的 `path`，段落之間不連線。驗證：以兩段落的資料呼叫，斷言產生 2 個 `path` 元素
- [x] 2.2 Y 軸範圍為 0 時退回固定最小範圍，曲線畫在垂直中央。驗證：以全天同高度的資料呼叫，斷言 `path` 的 `d` 屬性不含 `NaN`
- [x] 2.3 圖上標出最高與最低高度（公尺，整數）與 X 軸里程刻度。驗證：斷言 SVG 文字內容包含極值數字
- [x] 2.4 SVG 使用 `viewBox` 隨容器寬度縮放，並以該天的顏色繪製曲線（沿用 `day.color`）。驗證：斷言 `viewBox` 存在且 `stroke` 為該天顏色
- [x] 2.5 為 SVG 加上文字替代說明，包含日期、距離與高度範圍。驗證：斷言 SVG 內含對應的 `<title>` 或 `aria-label` 文字

## 3. 分天摘要的展開互動

- [x] 3.1 改寫 `src/ui/summary.ts`，把每一列包成 `<button aria-expanded>`，保留現有的 `Day N`、日期、顏色圓點與距離顯示不變。驗證：既有顯示內容的測試仍通過，且每列都有 `aria-expanded`
- [x] 3.2 實作一次只展開一天的手風琴行為，展開狀態存在掛載函式的區域變數、不進 `AppState`。驗證：測試涵蓋「展開一天」「切換到另一天」「再次點擊收合」「初始狀態」四個情境
- [x] 3.3 展開時掛載對應的剖面圖；該天資料不足時改顯示說明文字。驗證：測試涵蓋「完全沒有高度」與「其他天不受影響」情境
- [x] 3.4 確認 `state.subscribe` 觸發重繪時展開狀態重置。驗證：測試模擬變更時區與移除檔案後，斷言沒有任何列是展開的
- [x] 3.5 鍵盤操作：Enter 與空白鍵可切換展開，焦點樣式沿用既有的 `:focus-visible` 規則。驗證：測試派送鍵盤事件後斷言 `aria-expanded` 改變

## 4. 樣式

- [x] 4.1 在 `src/styles/components.css` 加上展開列與剖面圖容器的樣式，沿用既有 design token，不硬寫色值。驗證：`tests/tokens.test.ts` 仍通過
- [x] 4.2 窄螢幕（400px）展開後不出現水平捲軸。驗證：手動以 400px 視窗檢查，並確認 `.day-list > li` 既有的 `min-width: 0` 仍生效

## 5. 圖卡圖例改為月/日

- [ ] 5.1 在 `src/card/renderCard.ts` 把圖例標籤由 `Day ${d.dayIndex}` 改為該天的月/日，月與日不補前導零。驗證：測試涵蓋 `2026-03-05` → `3/5` 與跨月份 `3/30`、`3/31`、`4/1` 的情境
- [ ] 5.2 把 `/` 補進 `ensureCardFonts` 的預載樣本字串。驗證：斷言樣本字串包含 `/`
- [ ] 5.3 確認改動後的圖例在 5 天的行程中完整排得下，沒有因寬度而 `break`。驗證：以 5 天資料產生圖卡，斷言所有天都被繪製

## 6. 整體驗收

- [x] 6.1 執行 `npm test`，全部通過
- [x] 6.2 執行 `npm run build`，TypeScript 無錯誤
- [x] 6.3 以真實 GPX 手動驗收：上傳後展開各天確認曲線合理、缺高度的天顯示說明文字、下載圖卡確認圖例為月/日
- [x] 6.4 執行 `openspec validate day-elevation-profile --strict`，通過
